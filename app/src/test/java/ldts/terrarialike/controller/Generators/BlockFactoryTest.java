package ldts.terrarialike.controller.Generators;

import ldts.terrarialike.controller.generators.BlockFactory;
import ldts.terrarialike.exceptions.InvalidColorStringException;
import ldts.terrarialike.model.Block;
import ldts.terrarialike.model.BlockInfo;
import ldts.terrarialike.model.Position;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.Mockito;

public class BlockFactoryTest {

    @Test

public void generateBlockTest() throws InvalidColorStringException {
        /*
        Mockito.when(blockInfo.getBackgroundcolor()).thenReturn("red");
        Mockito.when(blockInfo.getForegroundColor()).thenReturn("blue");
        Mockito.when(blockInfo.getRepresentation_char()).thenReturn("A");

        Block block = Mockito.mock(Block.class);
        Mockito.when(block.getBlockInfo()).thenReturn();
        Position position = Mockito.mock(Position.class);
        Mockito.when(position.getX()).thenReturn(2);
        Mockito.when(position.getY()).thenReturn(88);

        BlockFactory blockFactory = new BlockFactory(1);

        Assertions.assertTrue(blockFactory.generateBlock(position,100).getClass().isInstance(Block));

    }

    @Test

public void
*/

}}
